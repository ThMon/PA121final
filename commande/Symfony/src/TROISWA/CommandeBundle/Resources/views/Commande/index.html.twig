<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="utf-8">
    <title>PHP</title>
    <link rel="stylesheet" href="{{ asset('css/style.css') }}">
</head>
<body>
    <section>
        <h1>Bons de commande</h1>

        <table class="standard-table">
            <caption>Liste des commandes</caption>
            <thead>
                <tr>
                    <th>Commande</th>
                    <th>Date de la commande</th>
                    <th>Date de livraison</th>
                    <th>Statut</th>
                </tr>
            </thead>
            <tbody>
                {% for order in orders %}
				    <tr>
                        <td>
                            <a href="{{ path('troiswa_commande_recap', {'id': order.getOrdernumber}) }}">{{ order.orderNumber }}</a>
                        </td>
                        <td>{{ order.orderdate.date|date('d/m/Y') }}</td>
                       	<td>{{ order.getShippeddate|date('d/m/Y') }}</td>
                       	<td>{{ order.getStatus }}</td>
                    </tr>
				{% endfor %}
            </tbody>
        </table>
    </section>
</body>
</html>